spring:
  application:
    name: dkserver-back
  profiles:
    active: saic-test
  cloud:
    nacos:
      config:
 		server-addr: 172.16.13.13:8888  #  配置中心地址
        username: nacos
        password: vecent123!@#
        file-extension: yaml # 指定配置文件类型
        namespace: ${spring.profiles.active}
        shared-configs:
          - data-id: common.yaml
            refresh: true
            group: common
