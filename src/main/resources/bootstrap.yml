#本地环境配置
spring:
  application:
    name: dkserver-back
  profiles:
    active: foton
  cloud:
    nacos:
      discovery:
        server-addr: 172.16.13.13:8848 # 注册中心地址
        ip: 172.16.11.210
        namespace: foton
      config:
        server-addr: 172.16.13.13:8848  # config地址
        enabled: true # 是否开启
        refresh-enabled: true # 是否开启自动刷新
        file-extension: yaml # 指定配置文件类型
        namespace: foton
        group: liujz