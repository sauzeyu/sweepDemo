spring:
  application:
    name: @project.artifactId@
  profiles:
    active: foton
  cloud:
    nacos:
      discovery:
        server-addr: 10.108.33.137:8888
        ip: 10.108.33.50
        namespace: ${spring.profiles.active}
      config:
        server-addr: 10.108.33.137:8888
        file-extension: yaml
        namespace: ${spring.profiles.active}
        shared-configs:
          - data-id: common.yaml
            refresh: true
            group: common
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl